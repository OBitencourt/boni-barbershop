
import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#422B0F',
        },
        secondary: {
            main: '#ffffff',
        },
        background: {
            default: '#422B0F',
            white: '#ffffff'
        }
    },
})

export default theme