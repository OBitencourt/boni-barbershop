
import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: 'rgba(222, 49, 99)',
        },
        secondary: {
            main: '#ffffff',
        },
        background: {
            default: '#fffff',
            white: '#ffffff'
        }
    },
})

export default theme